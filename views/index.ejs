<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>User list</title>
    
</head>
<body>
    <table>
        <tr>
            <th>名前</th>
            <th>email</th>
            <th>更新</th>
            <th>削除</th>
        </tr>
    </table>
    <!--こちらに基礎課題で言われてる内容を記述しましょう。ここから-->
    <p id="textTag"></p>
    <p id="text2"></p>
    <ul id="list"></ul>
    <ul id="maplist"></ul>

    <!--こちらに基礎課題で言われてる内容を記述しましょう。ここまで-->
    <script type="text/javascript">
        // 参考例
        let textTag = document.getElementById('textTag');
        let NUMBER = `<%= number %>`;
        textTag.innerHTML = NUMBER;
        //上にあるHTMLに<p>を追加しその情報を取得した変数text2
        let text2 = document.getElementById("text2");
        //変数TEXTに書いてあるないように合わせた、
        //後でapp.jsで代入するtextを`<%= %>`で挟む
        let TEXT = `<%= text %>`;
        //ブラウザ上に表示するためのinnerHTML
        //テキストを入れたい場所の変数.innerHTML = テキストの内容を入れた変数;
        //ここまで書いたらapp.jsに移動する
        text2.innerHTML = TEXT;
        // ここからはapp.jsで作成した値をここでブラウザに出力するように機能を実装していきましょう。上記の参考例を元にやってみましょう。
        // 基礎課題01:画面に表示する機能作りましょう。
        /*
        基礎課題02,03をやる前に
        配列とオブジェクトリストをapp.jsから取得するときに以下のやり方が必要になります。
        変数名は自由に決めてください。プロパティ名についてはapp.jsでオブジェクト内に格納したプロパティ名を使用してください。
        以下使用する際は<%#の「#」部分を「=」に変更するよう気をつけてください。%>
        let 変数名 = JSON.parse('<%# JSON.stringify(プロパティ名) %>'.replace(/&#34;/g, '"'));
        */

        // 基礎課題02:HTML内にulタグを作成し、liタグでapp.jsで作成した情報を画面に表示して下さい。
        //配列とオブジェクトリストをapp.jsから取得してくる
        let ITEMS = JSON.parse('<%= JSON.stringify(listText) %>'.replace(/&#34;/g, '"'));
        //上のhtmlの部分に書いたid名listのulの情報を所得する変数list
        let list = document.getElementById("list");
        //for文で繰り返して内容を表示するための記述
        for (let i = 0; i < ITEMS.length; i++){
            let textItem = `<li>${ITEMS[i]}</li>`;
            list.insertAdjacentHTML("beforeend", textItem);
        };
        // 基礎課題03:作成したオブジェクトリストをブラウザに出力して下さい。その場合以下の様に出力して下さい。
        // 例）
        // name: s.chiba
        // email: s.chiba@gmail.com
        let MAP = JSON.parse('<%= JSON.stringify(mapItem) %>'.replace(/&#34;/g, '"'));
        let mapList = document.getElementById("maplist");
        for (let i = 0; i < MAP.length; i++){
            let mapItems = `<li>"name:"${MAP[i].name}</li><li>"email:"${MAP[i].email}</li>`;
            maplist.insertAdjacentHTML("beforeend", mapItems);
        }
    </script>
</body>
</html>
